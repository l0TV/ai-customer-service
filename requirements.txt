# 拾汇商城 AI 智能客服服务依赖
# 安装： pip install -r requirements.txt
# 说明：torch / sentence-transformers / langchain 体积较大，
#      若本机已全局安装，可用 python -m venv --system-site-packages .venv 复用。

# --- Web 框架 ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# --- LangChain 生态 ---
langchain>=1.0
langchain-core>=1.0
langchain-community>=0.4
langchain-openai>=1.0
langchain-milvus>=0.3
langchain-text-splitters>=1.0
langgraph>=1.0

# --- 向量库与向量模型 ---
pymilvus>=2.5
sentence-transformers>=3.0
torch>=2.2

# --- 检索 ---
rank_bm25>=0.2.2
jieba>=0.42

# --- 文档解析 ---
pypdf>=5.0
pymupdf>=1.24
python-docx>=1.1

# --- 其它 ---
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.0

# --- Nacos 服务注册（供 Spring Cloud Gateway 发现）---
# 3.x 的导入名是 v2.nacos（不是 nacos），且 API 为异步；
# 代码同时兼容 2.x 的同步 API，见 app/core/nacos_registry.py
nacos-sdk-python>=2.0
